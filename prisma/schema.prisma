// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Category {
  id        Int       @id @default(autoincrement())
  name      String    @unique
  slug      String    @unique
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  products  Product[]

  @@index([slug])
  @@index([name])
}

model Product {
  id        Int         @id @default(autoincrement())
  sku       String      @unique
  name      String
  brand     String?
  urlKey    String?
  enabled   Boolean     @default(true)
  printed   Boolean     @default(false)
  categoryId Int?
  category   Category?  @relation(fields: [categoryId], references: [id], onDelete: SetNull)
  createdAt DateTime    @default(now())
  updatedAt DateTime    @updatedAt

  @@index([sku])
  @@index([name])
  @@index([brand])
  @@index([enabled])
  @@index([categoryId])
  @@index([createdAt(sort: Desc)])
  @@index([updatedAt(sort: Desc)])
  @@index([enabled, createdAt(sort: Desc)])
  @@index([brand, enabled])
  @@index([categoryId, enabled])
  @@index([printed])
  @@index([printed, createdAt(sort: Desc)])
  @@index([brand, printed])
  @@index([categoryId, printed])
}

// MODEL COMENTADO - Eliminar cuando se confirme que no se necesitan los datos en producción
// model QRHistory {
//   id        Int      @id @default(autoincrement())
//   productId Int
//   product   Product  @relation(fields: [productId], references: [id], onDelete: Cascade)
//   url       String   // URL del artículo en el ecommerce
//   qrUrl     String   // URL del PNG del QR en Cloudinary
//   createdAt DateTime @default(now())
//
//   // Índices para optimizar queries
//   @@index([productId])
//   @@index([createdAt(sort: Desc)])
//   // Índice compuesto para búsquedas por producto y fecha
//   @@index([productId, createdAt(sort: Desc)])
// }
